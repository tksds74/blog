# 手元から `npm run deploy` する時に使う。
# `npx wrangler login` でOAuthログインするなら両方とも不要。
#
# トークンに必要な権限は「アカウント:Workers スクリプト:編集」のみ
CLOUDFLARE_API_TOKEN=

# トークンが複数アカウントにアクセスできる場合のみ必須。
# 単一アカウントなら省略してよい。
CLOUDFLARE_ACCOUNT_ID=
